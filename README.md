# server：

```
$ npm install
$ npm start
```
打开浏览器`localhost:8300/`。   

## 定义mongodb地址为本地：

```
var mongodbInfor = {
	'host': 'localhost:27017',
	'port':  '27017',
	'database': 'monitorperhour'
};
```